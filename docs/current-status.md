# KFF 当前状态（唯一入口）

本页是接手人员**唯一**的当前状态入口。历史文档保留原日期与结论，不再作为当前依据。

## 1. 基线标识（先核对这三项，再引用任何结论）

| 项目 | 值 |
|---|---|
| 分支 | `codex/facebook-foundation` |
| 源码提交 | `891f399a9c81aafb2d960046f1e3214dd893bf68` |
| package 版本 | `0.1.53` |
| release_id | **未声明** —— 本轮修复仍在工作树中、未打包，因此不引用任何 0.1.53 既有产物的现场证据 |

> 同为 `0.1.53` 的不同包必须按 `release_id` 与适配器摘要区分；只凭版本号套用现场证据是错的。核对方式：`node --import tsx scripts/local-runtime.ts status` 会给出当前 `release_id` 与各组件 PID。

## 2. 唯一台账

当前缺陷与修复状态以 **[`docs/defect-ledger.json`](defect-ledger.json)** 为准，共 25 项，每项登记：编号、优先级、类别、实施批次、状态、改动文件、测试文件、**未验证范围**。

台账由 `npx tsx scripts/verify-planning.ts` 校验，它会：
- 用条目**重新计算**分类与状态统计，与声明值不一致即失败（禁止手写总数）；
- 校验每个被引用的改动/测试文件**真实存在**；
- 要求声明"已修复验证"的条目必须带测试或人工验证记录；
- 要求未完全关闭的条目必须写明剩余工作与未验证范围；
- 要求写明与旧审计的合计口径。

当前统计（由台账计算）：**25 项 = 已修复并验证 25**（全部工作单已落地；其中界面层断言需重建前端后才覆盖工作树，见上节第 1 条）。

## 3. 与旧审计的关系（不要相加）

- 旧文档 `docs/audit-ui-vs-server-20260916.md` 的 **64 项 / 已修复 8 / 剩余 56** 是**历史结论**，本页不沿用其总数，**不与本轮 25 项相加**，也**不得据旧数字计算完成率**。
- 旧"已修复"里已有两条被本轮**重新打开或部分重开**：
  - 旧第 3 条（任务创建表单初始化与过滤条件不一致）→ `KFF-B07`（**重新打开**：显示规则改了，初始化与切换账号仍用旧筛选）；
  - 旧第 6 条（采集结果导出 100% 失败）→ `KFF-B08`（**部分重开**：普通出口已修，固定快照出口仍是旧字段规则）。
- 旧文档剩余段的 56 项**尚不能逐条映射**（仓库内的旧文档副本无法枚举其剩余清单）。在逐条完成"确认/合并/关闭"之前，任何总数与完成率都**不可计算**，台账中该项标记为 `TO_VERIFY`。

## 4. R 项与"实现存在 / 真实验收范围"分开登记

历史上以 R01–R12 描述的能力，本轮**不重述为"已做/未做"**，一律分两列看待：

- **实现存在**：代码与测试在当前提交上存在（例如自动评论来源接续 `deriveCommentMonitors`、查 8 字归一化已在评分代码中、收件链路的局部隔离）。
- **真实验收范围**：是否在真实账号、真实浏览器、真实数据库上验收过 —— 本轮**没有**执行真实发送、真实账号或生产库操作，因此这些一律记"未验收"，不得因代码存在而写成已通过。

## 5. 本轮明确未验证的范围（不得当作通过）

- `next build apps/web`、`test:ui` 全量、以及**界面层结论性验证**：本地 Web 由 `next start` 提供的是**生产构建**，`--project=web` 的断言描述的是**被服务的 bundle**，工作树改动需重建后才生效。
- 需要数据库的 `exportDiagnostic` 端到端读回（B03 关闭条件之一）。
- B23 的干净电脑安装、无系统 Node 的交付包、AdsPower 未就绪、Stop 后重启、原数据库恢复等实机场景。
- 完整 Agent/Guardian、全部数据库迁移、支付/退款核心与其他第三方适配器：**未深审**，不从本页推导为安全。

## 6. 未关闭项

无未开始项：25 项工作单全部落地。仍需注意的是**验证层级**，而不是缺陷状态：

- 界面层断言（B07、B09、B10、B12、B13、B20、B21、B22 等涉及页面的项）只在**重建前端**后才覆盖工作树；当前 `--project=web` 断言描述的是被服务的生产构建。
- `KFF-B12`、`KFF-B13` 的界面接线已完成，但没有浏览器级断言；台账中已逐条登记。
- 需要数据库的 `exportDiagnostic` 端到端读回、B23 的干净电脑/安装包实机场景同样未执行。

## 7. 历史入口

- `docs/next-step-current.md`：**历史**（0.1.47 与手动选源时期），保留原日期与原结论，仅作历史参照。
- 其他按日期命名的交接与审计文档同样保留原基线，不作为当前依据。
