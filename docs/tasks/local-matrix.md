# 本地矩阵执行与消息闭环

当前目标依据：[2026-09-13目标复核与交接](../../KFF-目标交接-20260913.md)。当前运行与执行进度见[现在从哪里继续](../next-step-current.md)和[9月14日本轮测试](../../KFF-本轮测试-20260914.md)。交接中的0.1.7/未发布0.1.8状态已被后续实测替代，不再按旧快照重做升级、登录或首次Messenger设置；原完整目标不变。下列阶段记录保留各自时点。

授权：2026-09-12 用户要求沿“数据入库与筛选”对话确定的目标实施，不偏离目标。

交付链：持续发现 → 统一线索池与筛选 → 获准任务 → 指定本地账号环境执行 → 可核实回执 → Inbox → 接待 → WhatsApp 销售确认。主动发现与联系接待分别验收。复用现有工程；不新增支付、订单、退款业务。

2026-09-13 当前检查点：用户指定的9号「客户-32857-A」已由安装版0.1.7 Agent沿原环境命令核验为郑成功，正常关闭且本机/数据库回执一致，双身份在环境中心可见。推广自推过滤已部署：140项合同、12项隔离库测试通过，历史原文重放75→0，人工审核理由保留。本轮真实搜索执行错误、0条入库，失败关闭证明保留；不能称为新规则的真实新增入库验收。原13条线索、4条消息、WhatsApp移交和177个不可变Agent文件保留，系统在线，监控暂停、发送关闭。接着处理读取错误、多账号真实业务读取、受控新评论与安装交付。见 [9号身份检查](../evidence/second-real-profile-agent-check.json) 与 [推广筛选及失败记录](../evidence/promotion-screening.json)。以下历史检查点保留原时点范围。

2026-09-13 近期搜索检查点：0.1.7实际Agent读取1条今天的公开内容，原文及时间标签与独立页面一致；查询因后续窗口结束保持PARTIAL。该条为课程推广，规则75分经原人工审核排除，0个新增有效客户。首次空解析提前结束的失败保留，修正后读取成功页及其余阻塞页均确认关闭。129项合同与8项浏览器场景通过，既有线索和消息移交保留。9号及受控新评论测试仍等待用户前置条件；自动意图筛选和安装交付继续。见 [近期搜索与审核证据](../evidence/real-current-search.json)。

2026-09-13 最新备份检查点：最新冷备份18,191个文件经原命令新增的`--restore-only`模式在独立目录/端口恢复，92张表、10,165条记录与停库快照摘要一致，31条迁移和RLS标志匹配；副本正常关闭。运行库全表、四份原配置、176个Agent文件保留，生产构建后工作台和Agent在线。9号仍为用户窗口，原队列核验继续待释放；多真实账号、新到内容和安装交付仍待验。见 [最新恢复证据](../evidence/database-restore-latest.json)。

2026-09-13 最新冷启动检查点：正式启动器及原初始化入口使用PostgreSQL正常停库；原数据库冷启动、停止、第二次冷启动通过，92张表（排除心跳时间）、四份原配置和176个Agent文件保留。停库冷备份18,191个文件两次逐文件SHA-256一致，2项隔离数据库进程测试通过。当前系统在线，9号用户窗口仍Active；真实多账号原队列、新到内容和完整安装仍未完结。见 [数据库冷启动](../evidence/database-coldstart.json)。

2026-09-13 最新恢复检查点：空闲生产宿主下 Web、Worker、Agent 分别异常退出后显式恢复通过，其他组件不变；宿主退出后全部子进程退出再启动，业务数据和176个原Agent文件逐项保留。3项隔离进程测试覆盖丢确认后的父进程断开与原命令重传；不扩称真实发送中断。新增 `KFF-Recover.cmd`，当前生产 Web 与 Agent 心跳、环境中心恢复正常；9号原Agent检查仍待用户关闭现有窗口。见 [恢复证据](../evidence/local-runtime-recovery.json)。

2026-09-13 最新检查点：0.1.6 生产 Web、原 Worker、安装版 Agent 的整套正常启动/重复启动/停止/重启通过，重启前后六张既有业务表与配对配置一致；实际 Agent 回执丢确认等待及 Worker IPC 退出的两项隔离测试通过。重启后 1876794 通过原队列重新核验身份并关闭。第二真实环境 9「客户-32857-A」已核对 Profile `k1gw2kh9`，现有窗口中可见身份为郑成功 `61594334430750`，已独立登记；待用户关闭外部窗口后验证原 Agent 命令。当前 87 个账号、54 个环境，原 46 条外部潜客、31 条迁移保留。正常启停不代表崩溃自动恢复、开机自启或干净电脑安装。见 [整套本机运行](../evidence/local-runtime.json)。

2026-09-13 最新检查点：0.1.5 `c03d7cf6048d` 已安装并经原队列读取 10 条公开评论，原始日期与独立页面一致；最近 7 天筛选将全部历史评论排除新候选。2 条本机/数据库关闭证明匹配，既有 12 条线索、4 条消息和 WhatsApp 移交逐字段保留。129 项合同、14 项隔离库集成、7 项自有 DOM 场景及类型/代码检查通过，查询页日期、字段筛选和真实来源文案已实看。监控暂停、发送关闭，新到商业线索、多真实账号与整套运行环境仍待验。见 [时间筛选证据](../evidence/real-publication-time-filter.json)。

2026-09-13 当前检查点：一条明确获准的 WhatsApp 邀请已由原 Agent 提交，独立页面核对后经原 human_review 裁定确认同一动作，只有一次尝试。用户明确确认 WhatsApp 收到，移交 CONFIRMED、客户 HANDOFF_COMPLETE。最终0.1.4包 `26490623ce0a` 经原队列将用户新发“KFF 增量测试”入库，原己方消息只增加回显观察；Inbox为3条来信和1条邀请，第二轮新增0、重复4，关闭证明匹配，供应商Inactive。125项合同、21项DOM检查及类型/代码检查通过。监控暂停、发送关闭。见 [真实移交与增量证据](../evidence/real-messenger-whatsapp-handoff.json)。以下段落保留历史，状态表以本次范围更新。

2026-09-13 双向历史的控制端入库已补齐，10 项隔离库测试通过：自己的回执回显不重复创建 Message，原生人工回复单独记录，新来信与重复扫描正确区分，旧来信同意不可复用，冲突整页回滚。本次未改真实 DOM 读取器、未发送消息；LM-09 的双方页面读取和真实新消息增量仍待实机验收。见 [双向入库证据](../evidence/mixed-browser-history.json)。

最新实机进展见 [真实收件证据](../evidence/real-browser-inbox.json)：1876794已完成个人身份和PIN设置，用户测试会话的2条真实消息通过安装的0.1.2 Agent入库；第二轮新增0条、重复2条，两次关闭证明一致。宸均的WhatsApp目的地已配置且界面确认；未发真实邀请，销售接手未验收。完整日期与会话类型缺少证据时分别保留null和UNVERIFIED。下文实施记录按各自检查点理解。

2026-09-13 最新：0.1.3人工回复模板、同意记录及原队列单次许可已实施并通过本地验证；新版Agent对真实会话重复读取2条、无重复入库，关闭证明匹配。31条迁移已应用，发送关闭。LM-08仍待一条明确获准的真实邀请，LM-09仍待真实发送回执、混合历史增量和WhatsApp销售确认；LM-10整套安装与两真实账号试运行继续待验。见[准备证据](../evidence/real-browser-message-readiness.json)。下文保留历史检查点。

最新筛选已逐条核对9条真实评论的原始日期，全部是7月30–31日的历史咨询；原审核入口将其排除当前候选，原始数据及审核证据保留，0条确认为当前有效客户。见 [评论筛选证据](../evidence/real-comment-qualification.json)。这不等于新评论增量验收或真实联系执行。

## P0 本次盘点

- 当前工程是 Next.js 控制台、PostgreSQL 私有 kff schema、Worker、Agent 和独立 guardian。保留现有未提交的 Apify/获客改动。
- `apps/agent/src/main.ts` 已具备配对范围、快照校验、独占 Agent、提交意图日志、回执重传；`guardian-child.ts` 已具备父进程失联关闭和关闭证明。
- `packages/core/src/execution.ts` 已具备账号/环境租约、单 Agent 执行槽、发送前检查与未知结果隔离。不能另起一套任务队列绕开这些约束。
- 现有环境仅记录账号、Agent、profile_key、state；缺少配置版本、登录身份、运行状态、真正的打开/检查/停止入口。
- 合成页面执行器会启动持久 Chromium。真实分支仍使用 Graph API，不等于真实 Facebook/Instagram 浏览器模板已经可用。
- Apify、采集监控和线索筛选，Inbox、人工接管、模型草稿与 WhatsApp 移交均已有模块；下一阶段接入这些模块，不能把本地矩阵做成第二个客户库。
- 本次修改前 `node scripts/run-check.mjs typecheck` 成功。既有 417 项历史测试及 Apify 数据条数不视作本次复验。
- 用户已选择 AdsPower；本机检测到 AdsPower Global 8.6.3。实现正式本地 API 驱动与原生持久 Profile 基线，实网 Profile 需单独验证。没有读取到对话中单独生成的 32 张卡/43 项验收附件，不编造其内容或 R3 编号映射。

## 工作卡与顺序

| 卡 | 内容 | 状态 | 验收条件 |
| --- | --- | --- | --- |
| LM-01 / P0 | 复用盘点和差距 | 已盘点 | 上述代码依据、基线检查 |
| LM-02 / P1 | 环境配置、版本、身份、资源互斥 | 本地验证通过 | 跨品牌拒绝；配置变化使旧任务失效；运行中不可换绑 |
| LM-03 / P2 | 持久 Profile 驱动、受控登录/检查/停止 | 原生环境、AdsPower 生命周期及1876794/9号独立个人身份的实际Agent核验通过，两号本机/数据库关闭回执一致 | 两环境重启隔离；Agent 接入；关闭证明；代理不回退直连 |
| LM-04 / P3 | 恢复日志、租约、失联、人工接管 | 本地故障验证通过 | 重复命令不重开；旧上下文未关闭不能释放资源；多身份切换后的实际接管仍待验证 |
| LM-05 / P2 | AdsPower Profile 管理接口 | 1876794与9号已独立绑定；两个个人身份均通过原Agent核验和正常关闭 | 正式接口启动/停止/检查/CDP 与指定 Profile 一致 |
| LM-06 / P4 | 本地持续采集 → 现有线索池 | 真实搜索及评论定时重读已验；0.1.5 自动保存10条评论原日期并按最近7天排除历史咨询，原审核保留；新评论增量与长期运行待验 | 新评论入池、分页、增量、重复、退出保留 |
| LM-07 / P5 | 多账号调度和竞态 | 两个实际 Agent 的合成浏览器并发收发、隔离及重启恢复已测；1876794与9号真实身份已分别通过原Agent检查，跨账号业务读取、人工接管与真实调度继续待验 | 两账号独立、同账号串行、人工控制与任务不争用 |
| LM-08 / P6 | 单个批准的真实页面动作 | 一条真实邀请只提交一次；原UNKNOWN尝试保留，经独立页面核验和原human_review确认，未重发；新版即时自动发送回执仍待后续真实验证 | 指定身份、目标、正文、提交意图、可追溯页面证据 |
| LM-09 / P7 | 浏览器入站 → 现有 Inbox 与 WA | 指定真实会话3条来信、1条邀请；新来信增量与己方回显去重通过，第二轮新增0/重复4；WhatsApp到达及接手由用户明确确认；长期收件待验 | 增量/去重/回声；草稿失效；测试联系人回复与销售确认 |
| LM-10 / P8 | 两账号试运行、安装、升级和恢复 | 独立Agent包、跨版本日志、真实双向收件及日期采集、数据库升级回滚通过；整套正常启停、空闲组件/宿主故障显式恢复、原数据库冷启动/正常停止/再启动已实测，92张表及176个Agent文件保留。干净机器安装、整机故障、无人值守恢复与两真实账号试运行待验 | 实机演示和恢复证据 |

## 回滚与证据

0.1.0→0.1.1 验收对应具体 ZIP 版本，八条合成平台关闭证明及两条安装包实机评论回执分别记录；反向降级、任意版本兼容和真实消息发送不在此次结论中。见 [升级与实机证据](../evidence/agent-upgrade-runtime.json)。

新迁移先在随机隔离测试数据库运行，禁止清空开发库。实际开发库迁移前保存并验证本机备份。代码回退只处理本次修改，保留原未提交改动。已运行的 Profile、命令、未知结果和日志保留；回退代码不能自动释放未确认关闭的资源。

每张卡记录修改文件、实际命令、结果、未测项和下一张卡。本地原生浏览器通过、正式供应商通过、真实平台动作通过和销售接手分别标记；总目标尚未完成。

## 2026-09-12 本地环境检查点

实现：新增环境配置/命令合同和迁移，`environments.ts` 复用原账号/环境资源锁；Agent 的环境 guardian 使用专有目录、持久绑定和关闭日志；普通页面任务接入同一受管 Profile，运行、关闭及未知状态由已领取命令更新。新增 `/environments` 配置、检查、登录窗口、停止入口和 `adspower-inspect.ts` 只读诊断。文件指纹与命令记录见 [环境运行证据](../evidence/browser-environment-runtime.json)。

验证：126 项单元/合同全量通过；最后新增的 AdsPower 序号查询/凭据过滤与错误分类所在文件 4 项通过。四个相关集成文件首次 66 项通过，补充两 Agent 和状态测试后，环境/执行文件 38 项通过。新环境浏览器夹具 3 项通过，真实本机 Web → 数据库 → Worker → Agent → 持久 Chromium → 关闭回执通过。完整浏览器夹具批次中一个既有恢复用例因 15 秒等待超时失败，单独复跑通过；没有把失败的完整批次记成全绿。最终 TypeScript、ESLint、生产构建及 134 项旧任务账本校验通过。

迁移前冷备份校验了 8,740 个文件 SHA256；从副本实际启动独立数据库验证成功，账户 78、潜客 46、迁移 23。之后新迁移已应用本机开发库。旧数据和既有未提交获客改动保留。

实机边界：用户选择原有七环境中的序号 **195**，提供 `k1giuoa4`，已在 AdsPower 原生窗口核对同一行 ID。用户已明确授权安装150内核。界面下载进入队列后提示失败，随后从官方版本服务取得安装包；MD5与官方值一致，SunBrowser和chromedriver数字签名均有效。已新增独立 `chrome_150` 目录，版本 **150.0.7871.47**，保留148内核和7个环境配置。195经界面成功启动，Playwright连接与该Profile端点匹配，只读空白页证实 `en-US` 和 `America/New_York`。启动检查页提示代理失败，直接启动API仍返回供应商网络错误。停止调用在原有等待内未取得关闭证明，随后独立状态查询和原生窗口均确认已关闭，再释放本次检查专用锁；不能将其记作无人值守完整通过。平台账户登录与动作均未核验。

此处为195的历史故障记录；当前用户选择已改为1876794 / k1gvdtft，旧代理不再作为下一步。同时推进 LM-06 的浏览器分页采集接入现有 collection checkpoint 和线索池。采集仍须通过现有任务和 Agent 资源槽，不能在 Worker 内另开绕过租约的浏览器。真实账号动作、浏览器 Inbox、WhatsApp 销售闭环与两账号试运行均未完成。

补充故障边界：启动响应丢失时，即使暂时查询为 `Inactive`，也可能还有延迟启动，不能释放 Profile 锁。已修复并用真实本机 HTTP 故障夹具验证；环境浏览器夹具现在为 4 项通过（25.8 秒）。明确的供应商拒绝且状态未打开，与启动结果未知分别处理。

23:10检查点：正常退出空闲的AdsPower，核对三份管理配置备份后，在新进程中移除终端HTTP代理变量，管理API恢复并列出全部7个Profile。195的API编号实际是7，固定ID仍为 `k1giuoa4`。实机暴露了关闭延迟和状态查询限流，驱动已补充有限等待、查询间隔和实例变化拒绝。修正后的全API生命周期于15:00:00Z至15:00:05Z通过，版本150.0.7871.47、语言en-US、时区America/Los_Angeles。另一次只读Facebook访问报 `ERR_PROXY_CONNECTION_FAILED`，证实配置的本机SOCKS5端口10905无法连接；没有取得平台登录身份。该次关闭超过原观察期，随后独立查询确认Inactive。最后7项合同、4项浏览器回归、TypeScript与相关ESLint通过。新增启动脚本仅影响AdsPower新进程，原7个Profile配置及全局代理均未改动。LM-06尚未实施，不能把这些浏览器连通性测试当作持续采集验收。

## 最新环境选择与 P4

用户将后续联调环境改为 1876794，已在原生窗口核对对应 sjpdl-SG、k1gvdtft、API编号8。AdsPower当前为8.6.3 / 2.8.8.7。新环境的150内核全API生命周期通过，语言en-US、时区Asia/Singapore；Facebook可达但尚未登录。只读访问后的关闭超过观察期，独立状态查询于15:56:30Z确认Inactive。后续按 [当前Profile](../evidence/adspower-selected-profile.json) 继续，旧195代理故障留作历史。

P4新增LOCAL_BROWSER合成模板、固定环境查询快照、原任务队列中的每页只读任务、Agent可见DOM提取和同事务回执入库。停止会取消排队任务并通过心跳停止已领取任务，晚到页丢弃；重复回执不重复入库，读取失败可以按原游标恢复，租约未知仍隔离。真实平台模板和本地原始回执的保留期清理尚未实施，不能把合成DOM验证当作平台验收。

本机迁移前冷备份校验10,546个文件SHA256，共289,933,179字节，原账号78、潜客46、迁移24。已应用采集任务关联迁移并重启Web、Worker、Agent；原客户数据保留。浏览器工作台合成流程读取两页共4条记录，生成3条命中线索，监控保留为暂停状态。

最终复核：浏览器采集与原获客两文件19项集成通过，其中10项覆盖新链路；此前执行/采集/获客56项通过。单元与合同批次130项通过，新增浏览器合同及关联合同10项通过。工作台到Agent的实际合成浏览器流程1项通过（48.4秒），TypeScript、相关ESLint、生产构建及旧账本检查通过。证据和来源指纹见 [P4检查点](../evidence/browser-collection-runtime.json)。

## 2026-09-13 Agent采集回执保留期

每页任务固定查询到期时间，Agent在控制端离线时也先执行本地清理。入库与关闭确认后删除原文；期限内丢失确认继续重传完全相同的事件，期限后只提交无原文的终止结果。压缩关闭文件保留原证明摘要和nonce；缺少证明仍阻止接单。清理意图和文件原子替换支持中途崩溃恢复，不新增队列或数据存储。旧的“本地原始回执清理尚未实施”仅适用于上一检查点。

9项新日志单元测试、3项浏览器合同、8项实际Agent/guardian进程测试（含6项既有恢复）、10项浏览器采集集成通过。类型检查与相关ESLint通过。本机开发服务在确认空闲后更新，原148条命令均已关闭，其中2条旧合成分页回执已清理。实际平台身份和消息闭环继续按原目标推进；此处不能作为真实来源验收。最终运行记录见 [回执保留期证据](../evidence/collection-journal-retention.json)。

最终工作台回归通过（1项，运行22.5秒）：新增两页任务固定原查询期限，入库后清理原文。当前150条命令均已关闭，4份压缩关闭证明与控制端原审计一致，原文回执为0。旧潜客46条、迁移25不变，合成验收账号增加1个至82；测试监控保留为暂停状态。补上崩溃临时文件清理后，12项单元/合同、2项实际Agent进程检查与最终类型检查再次通过。

## 2026-09-13 浏览器Inbox入库层

新增一对一可见消息批次合同，提取原Facebook接收事务供内部复用。浏览器来源和会话标识独立于Webhook/PSID，但仍进入原Inbox、客户与会话表。批次按账号串行，任何消息冲突整批回滚；重读或显示名称变化不重复建档，同一会话更换对方被拒绝。原生人工回声和明确退出沿用现有处理，新增“Stop contacting me”退出表达。来源证据保存环境配置版本和观察摘要，不伪造官方API联系窗口。

本次31项既有收件/接待集成通过；初次新增用例发现退出规则漏识别和统计未限定测试账号，修复后8项浏览器入库集成、26项相关合同通过。当前内部入口尚未与Agent回执/游标推进相连，未实现真实平台页面读取和浏览器发送；下一步必须补这段，而不是增加更多无执行入口的模型或把当前结果称为闭环。使用边界见 [浏览器Inbox](../api/browser-inbox.md)。

本机冷备份11,453个数据库文件、306,058,432字节逐一SHA256匹配，配置副本也匹配，随后应用迁移至26。原Inbox界面显示内部合同注入的合成浏览器消息，1项浏览器界面验证通过（17.6秒），截图已检查；这一项没有读取平台DOM或走Agent收件。最终类型检查、相关ESLint通过。原46条潜客保留，合成测试账号新增1个至83；浏览器来源事件1条，真实来源0条。修改后的6张相关表仍启用RLS，实际消息触发器以调用者权限运行。记录见 [入库层证据](../evidence/browser-inbox-ingress.json)。


## 2026-09-13 浏览器Inbox任务链路

收件监控已接入原 Worker、任务/动作、Agent单执行槽与guardian，工作台支持保存、读取一次、持续收件与暂停。监控固定账号和环境版本、游标及期限；消息、回执及检查点一次提交，随后等待关闭证明确认才能读取下一页。关闭证明晚到也不会自动解除未知执行的隔离。当前模板只读取本机编写的合成会话DOM，正式1876794仍需已登录身份及真实模板，浏览器回复与WhatsApp销售确认未验收。

11项收件轮询集成覆盖真实浏览器进程分页、增量/重复、错账号、错误摘要、整批回滚、角色/品牌、双账号、连续轮询、暂停和过期隔离。与既有采集/执行/模板合计62项通过；补强隔离检查后11项再次通过。122项合同/回执日志测试通过。工作台到Agent再到Inbox的1项界面测试通过（28.3秒，含启动总31秒），并目视检查截图；与上一条仅Core注入的界面检查分开。

先冷备份12,675个数据库文件、331,642,785字节逐一SHA256匹配，并验证配置副本，再应用第27个迁移。当前账号84（新增1个合成验证账号）、原潜客46，浏览器消息事件5条均为合成。两页实际Agent日记均已删除原文，压缩关闭证明与原审计、检查点摘要一致。测试监控保留暂停、没有未关闭任务。两张新表启用RLS，应用角色不能修改既有检查点。详见 [收件轮询证据](../evidence/browser-inbox-polling.json)。
