# TASK-095：退款与调整，完成本地范围后暂停

基线提交 `ee2aa88`。依据规划 7.7、TASK-095、ACC-29 退款子范围实施；KFF 独立客户/订单/聊天/数据与第一阶段 Stripe 的选择保持不变。用户已明确实际 Stripe 配置后置，并要求“这个退款部分做完可以暂停了”。本次退款与调整代码完成、验证并保存后暂停，不继续 TASK-096。完整项目仍未交付，结束动作是保存并通知，不关机。

## 已保存的实现

第 17 个迁移 `20260912022322_g3_stripe_refunds.sql` 已应用本机开发库与隔离测试库，既有迁移没有改写。新增每笔付款的财务租约/额度账本、退款申请、Stripe 退款与争议投影、不可变观察、退款资金变动、争议实际余额变动、人工调整及关联冲销。现有和新增已核实付款自动获得账本。

管理员按原付款、版本、明确金额、已核对额度和依据申请部分或多次退款。本地预占与提供方占用共同控制剩余额度；处理中、补充操作和未知结果保留占用，失败或取消需经过原对象核对。执行前复查商户、模式、原 PaymentIntent/Charge、KFF 关联、完整退款/争议列表、原申请人与停止状态。

退款采用固定提供方参数和原请求幂等键。响应丢失先查询原退款；必要时仅在首次提交后的 23 小时内复用原键，超过窗口不重做。拿到退款 ID 先绑定再进行后续查询。每个付款的 90 秒 token 租约串行核验；回调、对象观察和资金变动同事务完成，旧回调只触发当前对象查询。成功后的退回/失败以新增反向资金条目记录，不能覆盖历史或重复扣减。

争议与实际币种的金额、手续费、净额独立记录，不自动猜测换汇。人工调整有明确金额、方向、依据和操作者，原记录不可改写，纠错只增加一次对应冲销。它们不会向 Stripe 转账或改变交付状态。普通 Worker 跳过合成连接，TEST 财务数据不计真实收入。

订单页面已接入退款概览、管理员申请、未提交撤销、原对象核验、处理中/失败状态、争议、调整/冲销和关联历史。暂停连接同时约束新的收款与退款，原对象继续核对。操作步骤和接口见 [退款与调整 API](../api/stripe-refunds.md)。

## 实际执行的检查

2026-09-12，**308 项测试、9 类检查全部通过**，无失败、跳过或浏览器重试。每类检查绑定同一组 **180 个源码/配置文件**，检查过程中源文件没有变化。最终源码摘要为 `0fe103cdb4a5949faded0306e884ce4dce58748493be3ca20edc051cc03401b2`；包含最后重启开发服务后生成的 `next-env.d.ts`。正式证据在 [检查点](../evidence/g1-local-checkpoint.json) 和 [任务账本](ledger.json)。

| 检查 | 实际命令 | 结果 |
| --- | --- | --- |
| 类型检查 | `node scripts/run-check.mjs typecheck` | 退出码 0 |
| 代码规则 | `node scripts/run-check.mjs lint` | 退出码 0 |
| 单元测试 | `node scripts/run-check.mjs unit` | 25 项通过 |
| 输入与提供方合同 | `node scripts/run-check.mjs contracts` | 43 项通过 |
| 隔离数据库集成 | `node scripts/run-check.mjs integration` | 205 项通过 |
| 执行器合成页面 | `node scripts/run-check.mjs fixtures` | 15 项通过 |
| 工作台浏览器 | `node scripts/run-check.mjs web` | 20 项通过 |
| 生产构建 | `node scripts/run-check.mjs build` | 退出码 0 |
| 独立端口生产冒烟 | `node scripts/run-check.mjs production` | 退出码 0 |

退款新增 **5 项合同、23 项集成和 1 项浏览器流程**。包含并发部分退款与额度竞争、满额多次退款、pending/requires_action/failed/canceled、成功后银行退回及再次成功、重复/乱序回调、签名模式与对象篡改、后台退款及超过 100 条分页、循环游标拒绝、已知对象补查、暂停/撤权、未知状态、数据库写入失败的事务回滚、不同币种争议费用及后续归还、不可变调整与一次冲销。

恢复测试既包含创建响应丢失、后续查询失败和旧租约拒绝，也实际终止了已被合成提供方受理、尚未收到响应的退款 Worker 子进程。重启后从原元数据找到退款，保持一笔提供方对象和一次创建调用，没有重复退款。另验证 23 小时外未知请求保留占用、外部退款抢占额度后停止提交，以及满额退款退回和替代退款按先回补再扣减处理。

真实安装的 Stripe SDK 对本地 HTTP 服务验证 API 版本、原付款展开 Charge、退款路径、整数金额、KFF 关联元数据、幂等键、列表的原 PaymentIntent 与分页游标。独立生产构建在端口 3001 验证登录、退款排队、处理中额度、原始签名 HTTP 回调和重放、当前对象核验、调整与冲销，以及测试收入隔离；未访问 Stripe 网络。

桌面与 390 px 页面验证了金额输入、确认、刷新、未提交撤销、调整、冲销及无水平溢出。已查看并保存 [退款流程](../../output/playwright/stripe-refunds.png)、[手机退款流程](../../output/playwright/stripe-refunds-mobile.png)、[保存后的订单概览](../../output/playwright/stripe-refunds-saved-overview.png)。其余既有流程的截图随完整回归刷新。

已刷新 Facebook 的本地代码合同证据，实际 Facebook 测试仍后置。原始规划和两个归档的摘要保持不变，任务总数 134、验收总数 72。最后在确认无未关闭命令、待执行任务或实际在途支付/退款后，重启了本次拥有的开发服务以加载新 Worker；健康接口返回正常，私密日志留在 `.kff`。

## 当前暂停与后续范围

TASK-095 标记为 `DONE_SCOPED`：代码和本地验证已完成。实际 Stripe 测试商户、API/签名密钥、外部端点投递、真实失败与争议样例、资金到账、密钥轮换及生产网络验收按用户要求后置。没有配置实际账号、创建公网 Stripe 端点、产生真实收退款或执行关机。

TASK-096 交付与恢复尚未实施，不能据此宣称完整 ACC-29、E2-MIN、G3 或整个规划完成。任务账本记录 `PAUSED_BY_USER`，之后需用户继续指示才开展后续工作。

保留原付款、未知申请、额度预占和核验证据。回滚先停止新创建并保留查询能力；数据库纠错使用新迁移，账目纠错用带依据的新记录，不能改写已应用迁移或删除未知资金状态。
