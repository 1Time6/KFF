# TASK-053：结果筛选与精确目标快照

基线 `0eb7c78`。按规划 26.4 实现当前页、手选行及全部筛选结果的明确选择，使用服务端实际结果生成不可变对象/观察版本快照，记录用途、允许字段及排除原因。

本阶段接入已有合成与人工来源，复用查询、观察、品牌权限和幂等规则。筛选条件在服务端求值；当前页绑定结果页证据，手选逐 ID 校验，全筛选基于全部持久结果。之后翻页、查询增量和更改筛选不能扩大已保存范围。预览变化需重新核对，过期观察不进入新目标集。

快照作为后续任务拆分输入，并不直接授予发布或联系权限。现有来源允许用途为软件验证/数据核对；创建具体平台任务前仍需来源用途、对象语义、联系依据、退出、平台能力、模板与预算的共同门槛。消息动作及 TASK-060/068 的任务联调按其依赖继续完成，不以对象列表冒充已发送任务。

验证：当前页/手选/全筛选区别、分页与增量、筛选变化、重复与并发保存、错误作用域和观察版本、用途排除、到期与撤销、浏览器完整选择流程；原始计划及全范围台账保留。

实现文件：`packages/contracts/src/target-selection.ts`、`packages/core/src/collection-filter.ts`、`packages/core/src/target-snapshots.ts`、固定导出分支、相关 API/工作台、Worker 到期清理及新增迁移 `20260911221017_g2_target_snapshots.sql`。API 和选择语义分别见 `docs/api/target-snapshots.md` 与 ADR-015。

新增 2 项合同、12 项隔离数据库、1 项浏览器完整流程；另为已有导入确认增加 1 项请求 ID 冲突回归。生产构建覆盖筛选预览、保存、固定导出和撤销。数据库验证翻页/增量不扩张、旧观察不替换、锁协调、RLS/复合外键、过期内容清理及审计。浏览器分别保存当前页 25 个、跨页手选 2 个、全筛选 16 个，更改筛选后仍导出原 16 个对象，并验证撤销。

状态：已实现，完整回归以最新 `docs/evidence/g1-local-checkpoint.json` 的实际退出码、时间和源码摘要为准。当前只覆盖数据准备和固定导出，不替代具体任务联调或真实平台验收。

回滚：停止本项目执行进程后回退代码；保留已保存快照、数据库及审计。迁移已应用，不修改或删除原迁移，结构回退需新增迁移；不可因回退而扩大旧快照或恢复已撤销选择。
