# TASK-059：计划规则与时区

基线 `8075719`。依据规划 9.4、UI-10、ACC-34/68 实现有限日期范围内的一次、每日和每周当地规则，保存 IANA 时区、明确的重复/缺失时刻处理和错过时点策略。

本阶段建立规则预览、不可变版本、UTC 日历、下一时点、可靠的时点准备记录，以及暂停、恢复和终止未来时点。夏令时与错过策略必须显式选择；补准备有数量上限和间隔。规则更新需要新版本并重新核对，原版本和原时点保留。当前只准备时点，不产生执行许可或平台动作；TASK-060 将其与逐账号/目标任务及共同批准、预算、停止门槛组合，不绕过原执行内核。

范围：公共计划合同、规则求值、数据库迁移与品牌隔离、计划服务、Worker 准备循环、API、`/schedules` 工作台及相应测试。默认保存为暂停，用户显式开始时点准备。当前真实 Facebook 执行继续后置。

验证：春季缺失、秋季重复、非整小时变化、日期边界、真实零/非法日期、跳过/仅延后最近一次/限量补准备、并发与重放、暂停/终止/新版本、实际进程提交前后终止、跨品牌、页面与生产构建。具体平台任务及真实日历执行不计为本次已验。

实现：`packages/contracts/src/schedule.ts`、`packages/core/src/schedule-rules.ts`、`packages/core/src/schedules.ts`、迁移 `20260911224129_g2_schedule_rules.sql`、Worker、API 和计划页面。字段与边界见 `docs/api/schedules.md`、ADR-016。

新增 9 项规则单元、2 项输入合同、10 项隔离数据库与 1 项浏览器流程。隔离数据库包含实际进程在提交前后终止、并发准备、暂停/恢复/终止、版本保留、时区数据更新阻断和品牌隔离。浏览器核对纽约两次相同当地时刻的不同 UTC 点，再调整版本、生成限量准备、暂停及终止。生产构建检查预览、暂停保存和终止。最终结果按最新本地检查点源码摘要及退出码记录。

状态：代码及独立验证已完成，完整回归以 `docs/evidence/g1-local-checkpoint.json` 为准。实际平台任务与预算执行联调尚未完成；该部分继续留在 TASK-060 与对应关口，不计全范围通过。

回滚：停止本项目 Worker 后回退代码；保留数据库、版本和时点记录。已应用迁移不修改或删除，结构回退通过新增迁移进行；不得回拨已提交游标或重新生成原时点。
