# TASK-054 联系依据基础子范围

基线：`d33779b`；用户授权仍为按规划实施、Facebook 首平台、真实测试后置。

本次实现独立于采集器的联系目标、来源/用途/窗口依据、退出、明确新同意恢复、不可变依据版本，以及可在提交事务中复用的检查。平台资格、预算、审核、租约和停止门槛仍由共同执行内核负责；此组件返回“依据满足/不满足”，不自行授权发送。现有 E1 为主页发布，因此本次不添加真实消息动作。

允许范围：新增联系合同、核心服务、新迁移、API 路由、隔离数据库测试及实施文档。不修改原规划，不复制真实联系人，不向平台发送请求，不假设固定消息窗口时长。

检查：作用域与身份字符串、并发幂等、用途错配、未知/失效来源、显式窗口、退出后的旧快照、恢复必须基于新的明确同意，以及退出与最终检查的锁顺序。后续 TASK-080 接入具体消息动作后仍需验证完整发送链。

回退：回退新增入口和调用代码，保留已登记依据与退出历史；通过新增迁移调整数据库，不直接改写已应用版本。
