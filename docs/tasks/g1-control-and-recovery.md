# G1 后续实现范围

前一代码检查点：`3ed148f`。沿用用户 2026-09-12 授权：按规划直接实施，Facebook 首平台，真实测试后置。

当前任务：TASK-028/029/030/032/033/038。补齐组织/账号停止开关、Agent 登记/撤销/排空、关闭证据和 D0 元数据，继续建立真实进程故障的合成测试。真实 Facebook、真实设备和付费服务均不在本次副作用范围。

允许文件：`packages/contracts/src`、`packages/core/src`、`apps/agent/src`、`apps/worker/src`、`apps/web`、新增 `supabase/migrations`、`scripts`、`tests`、`docs/tasks`、`docs/evidence`。不修改已经应用的迁移、原始规划和其他项目。

前置：62 项本地测试及生产 Web 启动检查已通过，见 `docs/evidence/g1-local-checkpoint.json`。此结果只涵盖记录中的子范围。

停止操作必须与最终提交门槛通过数据库锁排序协调：先提交的动作作为在途继续核验；先生效的停止拒绝新提交。未领取的命令可以由服务端证明尚未启动；已领取命令保持 guardian 关闭证明要求。

验证：新增并发停止/提交竞争、Agent 撤销/排空、未领取与已领取命令恢复差异、诊断版本/耗时与权限测试；运行适用类型检查、lint、集成及工作台流程。所有实际命令与结果另写证据，未运行不标完成。

回退：先停止本项目进程，回退新代码；数据库通过新增迁移兼容处理。保留在途 journal、租约和未知动作，不通过回滚重发。用户及独立审核尚未签收。
