# TASK-006：现有站点候选盘点与待确认范围

2026-09-12。KFF 工程检查点 `3906d29` 已保存，225 项本地测试、9 类检查通过；完整规划、真实 Facebook、设备及最终交付仍未完成。

从 Codex 已登记项目列表发现两个与规划“复用站点、客户、订单、接待”有关的本地工程，仅定向读取说明、包配置、类型、迁移声明和相关 API 源码。没有读取 `.env`、客户数据或生产凭据，没有运行其脚本或访问线上站点，也没有改动其文件。

| 候选 | 实际工程与已有代码证据 | 当前不能推定的事项 |
| --- | --- | --- |
| EasternWisdom | `C:/Users/17731/Desktop/fb`；README 和 package 名表明是独立站，并非由目录名可以确定的 Facebook 自动化项目。包含报告/商品订单、支付事件、接待工作区；`app/api/admin/orders/route.ts` 读取经过后台授权的订单，`app/api/messages/route.ts` 使用接待权限及 `contact-store`。当前读取的 Git 提交为 `fe8a312bfac0c20dd53407aeff262617e3adaddd`。 | README 中部署声明未在线核验；尚未确认是否作为 KFF 首个业务系统、对应哪个品牌、采用什么集成授权。旧数据模型文档部分启动描述与当前 README 不同，后续以实际代码和运行验证继续核对。 |
| 珍珠站 Pearl | `C:/Users/17731/Desktop/pearl (3)/pearl`；包描述为双语珍珠电商。存在 Supabase profiles/orders/order_items 迁移、订单/退款类型、受后台权限保护的订单导出，以及 Tidio 组件文件。旧 `/api/orders` 默认禁用并指向 `/api/paypal/create-order`。 | 尚未确认 KFF 接入范围、品牌、接待系统是否沿用 Tidio、部署与数据状态。不能把旧订单路由作为现成创建接口，也不能把文件存在当作已联调。 |

仅发现两个独立业务候选，不能自动把它们归并为同一客户或订单主数据。KFF 尚未新建对应客户/订单主库，未选择其中一个作为权威来源。Video OS、WhatsApp 号池、原始附件和其他设备供应商信息仍缺直接证据。

按照规划 8.3、TASK-006/087/090 的复用要求及用户“拿不准暂停询问”的指令，下一步需确认：**KFF 要复用哪个现有站点的客户、订单与接待系统，以及是否有多个站点需各自按品牌接入。** 在范围明确前暂停继续实施；不把代码先写、真实 Facebook 测试后置的授权解释为可以自行挑选或合并业务主数据。

本次只读来源的路径、字节数与 SHA-256 存在 `docs/evidence/existing-systems-readonly.json`。其他工程测试未运行，不计入 KFF 的 225 项测试；线上身份、客户和支付均未访问。目标未全部完成，不执行关机。
